# ═══════════════════════════════════════════════════════════════
#  FretForge Server — Environment Variables
# ═══════════════════════════════════════════════════════════════

# PostgreSQL connection string
# Local: postgresql://user:password@localhost:5432/fretforge?schema=public
# Railway: Provided automatically via Railway's PostgreSQL plugin
DATABASE_URL="postgresql://user:password@localhost:5432/fretforge?schema=public"

# Server port (Railway assigns this automatically)
PORT=3001

# JWT secret for authentication tokens
# Generate one: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="change-me-to-a-real-secret-in-production"

# Frontend URL (for CORS configuration)
# Local: http://localhost:5173
# Production: https://fretforge.dev (or wherever Vercel deploys)
CLIENT_URL="http://localhost:5173"

# Node environment
NODE_ENV="development"
